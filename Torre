#include <stdio.

(int n, char origem, char auxiliar, char destino) { if (n == 1) { printf("Mova o disco 1 da haste xc para a haste %c\n", origem, destino);

}

return;

torreHanoi (n 1, origem, destino, auxiliar); printf("Mova o disco %d da haste so para a haste %c\n", n, origem, destino);

torreHanoi (n 1, auxiliar, origem, destino);

}

Svoid resolver TorreHanoi (int numDiscos, char origem, char auxiliar, char destino) {

torreHanoi (numDiscos, origem, auxiliar, destino);

int main() {

int numDiscos;

char origem = 'A', auxiliar =

Stop

Share

printf("Digite o número de discos: "); scanf("%d", &numDiscos);

print("Passos para resolver a Torre de Hanói:\n"); TorreHanoi (numDiscos, origem, auxiliar, destino);

resolver

return 0;

